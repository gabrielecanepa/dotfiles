#!/bin/sh

. "$(dirname "$0")/init"


commitlint --from=HEAD --to="$(git remote)/$(git branch --show-current)"
run shellcheck
prettier --check ".{commitlintrc,prettierrc}" ".{config,github,npm,vscode}/**/*.{css,json,md,yml}" --ignore-path .gitignore --ignore-unknown
