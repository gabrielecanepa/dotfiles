# google plugin

Searches Google in the default browser, or opens the homepage with no query.

To use it, add `google` to the plugins array in your zshrc file:

```zsh
plugins=(... google)
```

## Usage

```zsh
google          # open google.com
google <query>  # search the given query
```
