# colors256 plugin

Prints the 256 xterm color codes, each on a swatch of its own color, 16 per row.

To use it, add `colors256` to the plugins array in your zshrc file:

```zsh
plugins=(... colors256)
```

## Usage

```zsh
colors256
```
