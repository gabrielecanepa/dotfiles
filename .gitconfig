# Core
[include]
  path = ~/.gitprofile
[core]
  excludesfile = ~/.gitignore.global
  ignorecase = false
  pager = less -FRSX
[init]
  defaultBranch = main
[pull]
  rebase = true
[help]
  autocorrect = 1

# Branches 
[branch "main"]
  mergeoptions = --no-edit
[branch "master"]
  mergeoptions = --no-edit

# Theme
[color]
  branch = auto
  diff = auto
  interactive = auto
  status = auto
  ui = true
[color "branch"]
  current = green
  remote = yellow

# Commands
[filter "lfs"]
  clean = git-lfs clean -- %f
  process = git-lfs filter-process
  required = true
  smudge = git-lfs smudge -- %f

# Aliases
[alias]
  a = add
  aliases = !git config --get-regexp \"alias.*\" | sed \"s/alias.//\"
  br = branch
  ci = commit --interactive
  cm = commit -m
  co = checkout
  df = diff
  ds = diff --stat -r
  f = fetch
  fo = fetch origin
  get = !git pull --rebase && git submodule update --init --recursive
  lg = log -10 --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset'
  log = log --color --graph
  p = push
  pf = push --force
  po = !sh -c 'git push $1 --set-upstream origin $(git branch --show-current)'
  ps = !git pull && git sweep
  re = remote
  remote = remote -v
  serve = !git daemon --reuseaddr --verbose  --base-path=. --export-all ./.git
  st = status -sb
  sweep = !git branch --merged master | grep -v 'master$' | xargs git branch -d && git remote prune origin
  undo = reset --hard
  unstage = reset HEAD --
  untrack = rm --cache --
  zip = !sh -c 'git archive HEAD -o $(basename $PWD).zip'
