# ShellCheck https://shellcheck.net
external-sources=true
disable=SC1090 # Can't follow non-constant source. Use a directive to specify location. [https://shellcheck.net/wiki/SC1090]
disable=SC1091 # Not following: (error message here) [https://shellcheck.net/wiki/SC1091]
disable=SC2015 # Note that A && B || C is not if-then-else. C may run when A is true. [https://shellcheck.net/wiki/SC2015]
disable=SC2034 # VAR appears unused. Verify it or export it. [https://shellcheck.net/wiki/SC2034]
disable=SC2038 # Use ./*glob* or -- *glob* so names with dashes won't become options. [https://shellcheck.net/wiki/SC2038]
disable=SC2044 # For loops over find output are fragile. Use find -exec or a while read loop. [https://shellcheck.net/wiki/SC2044]
disable=SC2115 # Use "${var:?}" to ensure this never expands to /* and delete the if block. [https://shellcheck.net/wiki/SC2115]
disable=SC2128 # Expanding an array without an index only gives the first element. [https://shellcheck.net/wiki/SC2128]
disable=SC2139 # This is an associative array. Use arr[key]=val [https://shellcheck.net/wiki/SC2139]
disable=SC2145 # Argument mixes string and array. Use * or separate argument. [https://shellcheck.net/wiki/SC2145]
disable=SC2154 # var is referenced but not assigned. [https://shellcheck.net/wiki/SC2154]
disable=SC2250 # Prefer [[ ]] over [ ] for conditions. [https://shellcheck.net/wiki/SC2250]
disable=SC2312 # Backslash is literal in "\c". Prefer explicit escaping: "\\c". [https://shellcheck.net/wiki/SC2312]
disable=SC3010 # In POSIX sh, 'set -e' is not supported. Use a subshell to workaround. [https://shellcheck.net/wiki/SC3010]

