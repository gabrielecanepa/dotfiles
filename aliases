# Get external or local IPs
alias ip="curl ipinfo.io/ip"
alias ips="ifconfig -a | perl -nle'/(\d+\.\d+\.\d+\.\d+)/ && print $1'"
alias speedtest="wget -O /dev/null http://speedtest.wdc01.softlayer.com/downloads/test10.zip"

# Serve the current directory as HTTP
alias serve="ruby -run -e httpd . -p 8000"

# git
alias gsts="git status -s"
alias ga="git add"
alias gcm="git commit -m"
alias gco="git checkout"
alias gpom="git push origin master"

# Directories
alias code="~/code"
alias cdp="~/code/gabrielecanepa"
alias cdlw="~/code/lewagon"
alias tmp="~/code/tmp"

# Atom
alias atom="open -a Atom"
alias at="atom $1"
alias att="atom ."

# Shortcuts
alias aliases="at ~/.aliases"
alias ll:s="du -sh * | sort -hr"

# Updates and cleanups
alias brew:up="brew update && brew upgrade && brew cleanup; brew doctor"
alias node:cl="find . -name "node_modules" -exec rm -rf '{}' +"
